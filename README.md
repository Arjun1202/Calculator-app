# Calculator Application


You can use the command npm run dev or node storing.js to start the server.

The server will start on http://localhost:8383

You can get the result of the required calculations and the top 8 recently calculated results will be displayed on the website and stored in a local MongoDB database.
